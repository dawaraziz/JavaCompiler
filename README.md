 # Joos Compiler in Java
 
 
 ## Build
 
 - run make in the home directory
 - Requires ant:
 - Mac:
    - Brew install ant
 
 ## Executable
 
 Run bash script joosc.sh with a file as a parameter. This will call 
 the compiled jar file
 
 example:
 `./joosc.sh src/tests/test.txt`